this installation and config is tested on a ubuntu14.04 server.
type:
	sh install.sh

to start the batch process.
After the batch process you could use the weblogin:

	http://your_ip_address:8080/manager/html/ 

with following credentials:

	user: 	admin
	pass:	secret
